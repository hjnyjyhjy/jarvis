# Jarvis Ultra Lite — مواصفة تطبيق المساعد الصوتي الخفيف

ملخص سريع
- **الاسم:** Jarvis Ultra Lite
- **الهدف:** مساعد صوتي خفيف، سريع وموفّر للطاقة، قابل للتطوير على أجهزة تطوير ضعيفة (VS Code فقط).
- **الفكرة الأساسية:** Local-first & Light-first — لا معالجة مستمرة ولا تحميل مكتبات ثقيلة، كل شيء عند الطلب.

المتطلبات الأساسية
- لا Android Studio، لا Emulator، ولا Gradle builds محليًا على جهاز المطوّر.
- البِناء والاختبارات يتمّان عبر **GitHub Actions** في السحابة.

القيود على الجهاز المحمول
- لا Background services، لا Wake locks، لا Continuous listening.
- استخدام الميكروفون فقط في وضع **Push-to-talk**.
- إغلاق الموارد فور الانتهاء وإدارة استهلاك البطارية.

البنية المقترحة للتطبيق
- **Single Activity (Compose)** أو واجهة XML خفيفة — شاشة واحدة فقط.
- زر واحد: 🎤 **اضغط للتحدث (Push-to-talk)**.
- TextView واحد/منطقة عرض للردود.
- موديولات خفيفة: Audio (STT), TTS, Commands (rule-based), Memory (SharedPreferences)، Feature Flags.

الميزات الأساسية
1. **Speech Recognition (Push-to-talk)**
   - استخدام Android `SpeechRecognizer`.
   - دعم العربية والإنجليزية (اكتشاف اللغة من المدخلات).
2. **Rule-based Command Parser**
   - أوامر بسيطة قابلة للتمديد (فتح تطبيق/إعدادات، حالة البطارية، وقت الآن، WiFi/Bluetooth).
   - ردود آمنة عند عدم الفهم.
3. **Text-to-Speech**
   - Android `TextToSpeech`، يطابق لغة المستخدم تلقائيًا.
4. **ذاكرة خفيفة**
   - حقائق بسيطة محفوظة في `SharedPreferences` تحت مفتاح واحد (مثلاً `fact_last`).
5. **فهرسة نصية بسيطة**
   - key → value، تحميل عند الطلب فقط.

حالات تشغيل
- Normal (افتراضي)
- Power Saving (مكثّف قيود حفظ الطاقة)
- Developer Mode (نصّي + logging محدود)

الخصوصية والأمان
- التصرّف محليًا افتراضيًا — لا بيانات تُرسَل للسحابة إلا عند تفعيل ميزات ثقيلة عبر Feature Flags.
- تشفير اختياري للبيانات الحساسة (عند الحاجة).

معايير القبول
- زر Push-to-talk يعمل ويحوّل الكلام إلى نص بالعربية/الإنجليزية.
- تنفيذ وتحقق من 10 أوامر rule-based مع رد صوتي ونصي.
- الذاكرة الخفيفة تعمل (حفظ واسترجاع) باستخدام `SharedPreferences`.
- حجم التطبيق ووقت استهلاك البطارية ضمن الحدود المعقولة.

ملاحظات تنفيذية
- لغة التطوير: **Kotlin**, بنية MVVM خفيفة.
- اعتماد على مكتبات Android الأساسية فقط (SpeechRecognizer, TextToSpeech).
- تم استخدام Feature Flags لإبقاء الميزات الثقيلة مغلقة افتراضيًا.

ملحق: نقاط توسعة مستقبلية
- دعم AI خارجي عبر API (cloud-only) عند تفعيل Flag.
- تمييز المستخدم (Speaker ID) عبر خدمة سحابية/خارجية إن لزم.

---

هذه الوثيقة قابلة للتحويل إلى خطة تنفيذية خطوة بخطوة، خريطة ملفات (file map) أو Roadmap تطوير على حسب تفضيلك.
